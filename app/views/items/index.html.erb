<h1>Vous pouvez trouver tous nos produits ci-dessous </h1>


  <div class="row row-cols-4 row-cols-md-3 col-sm-12 p-md-0 m-md-0 d-flex justify-content-center">

  <div class="card-group mb-5" >


<% @items.each do |item| %>

  <div class="row row-cols-4 row-cols-md-3 col-sm-12 p-md-0 m-md-0 d-flex justify-content-center">

  <div class="card" style="width: 18rem;" >
  <img class="card-img-top" src= <%=  item.image_url %>
  <div class="card-body">
      <h5 class="card-title"> <%= item.title %> </h5>
    <p class="card-text"><%= item.price %> € </p>
    <a href="<%= item_path(item.id) %>" class="btn btn-primary" >Voir plus </a>
  </div>
</div>
 <% end %>